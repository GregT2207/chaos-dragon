services:
  node:
    build: .
    ports:
      - target: 3000
        published: 0
        protocol: "udp"
        mode: "host"
    scale: 3
    environment:
      HOST_NAME: "node"
      DISCOVER_SIBLINGS_INTERVAL_MS: 2000
      SIBLING_EXPIRY_TIME_MS: 6000
