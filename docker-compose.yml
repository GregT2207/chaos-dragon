services:
  node:
    build: .
    ports:
      - target: 3000
        published: 0
        protocol: "udp"
        mode: "host"
    scale: 3
    environment:
      HOST_NAME: "node"
      SCAN_SIBLINGS_INTERVAL_MS: 20000
